import 'package:flutter/material.dart';

class ShowSubscribersPage extends StatelessWidget {
  const ShowSubscribersPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

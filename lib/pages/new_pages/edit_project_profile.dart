import 'package:flutter/material.dart';

class EditProjectProfile extends StatelessWidget {
  const EditProjectProfile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
